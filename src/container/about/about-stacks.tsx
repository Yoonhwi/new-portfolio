import { darkBgColor } from "@/constants";
import { Box } from "@chakra-ui/react";

const AboutStacks = () => {
  return (
    <Box minH={"100vh"} backgroundColor={darkBgColor}>
      123
    </Box>
  );
};

export default AboutStacks;
